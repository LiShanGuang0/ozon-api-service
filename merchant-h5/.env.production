# If Nginx serves merchant-h5 and proxies /api to the backend, keep this as /api.
# If the API is on another domain, use the full address, for example:
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=/api
